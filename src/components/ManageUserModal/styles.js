const styles = theme => ({
  formContainer: {
      padding: theme.spacing.unit * 4,
      marginTop: '10%',
      width: "50%",
      margin: "0 auto",
      minWidth: 200,
      height: "auto",
      maxWidth: 500
  }
})

export default styles